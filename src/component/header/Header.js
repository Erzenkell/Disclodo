import React from 'react';
import { StyledHeader } from './StyledHeader';

const Header = (user) => {
    return (
        <StyledHeader>
            <h1>Header</h1>
        </StyledHeader>
    )
}

export default Header;